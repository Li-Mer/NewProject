// const { da } = require("element-plus/es/locales.mjs");

const config = {
  port: 3000,
  database: {
    host: "localhost",
    user: "root",
    password: "123456",
    WALL: "WALLTS",
  },
};

module.exports = config;
